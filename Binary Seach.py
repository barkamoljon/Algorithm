def binary_search(list, item):
  low = 0
  high = len(list)-1
  while low <= high
    mid = (high + low) // 2
    gues = list[mid]
    if guess == item
    return mid
    if gues > low:
      high = mid - 1
    else:
      low = mod + 1
  return None
    
      
